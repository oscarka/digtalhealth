[phases.setup]
nixPkgs = ["python39", "ffmpeg", "pip"]

[phases.install]
cmds = ["python -m pip install --upgrade pip", "pip install -r requirements.txt"]

[phases.build]
cmds = ["echo 'Build phase completed'"]

[start]
cmd = "/app/start.sh"
